export interface usuarioCadastrarDto{
    nome: string;
    email: string;
    senha: string;
}

//caso queira que o dado seja opcional basta colocar ? na frente
/* nome?: string;
email?: string;
senha?: string; */